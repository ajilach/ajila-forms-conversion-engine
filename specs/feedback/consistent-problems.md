# Consistent Problems

Systemic defects that affect **many forms in the same way** — a wrong fragment
used everywhere, an old-engine bug since fixed, a layout convention that changed.
Distinct from [resolved.md](resolved.md), which records one-off, per-feedback-item
resolutions.

Each entry is **data** the `/sweep` engine executes — symptom, how to find
affected forms, how to fix one. The engine is problem-agnostic: it dispatches on
the `Detect` / `Fix` verbs below and has no per-problem logic. A new entry is
**code-free only when it can reuse an existing `Detect`/`Fix` script**; a new
shape (a structural rewrite, an attribute target no regex matches) needs a new,
committed, reviewed `find_`/`set_` script first. See `.claude/skills/sweep/SKILL.md`
→ "Record a new problem".

**Establishing "correct":** a sweep needs a ground-truth reference. The conversion
engine is **not** authoritative by default (it has known faults) — rely on it only
when the user says it's right for this problem; otherwise ask for a reference.

## Entry format

```markdown
## PROBLEM-<slug> — <title>
**Symptom:** what's wrong, and where it shows up in the form .content.xml
**Detect:** grep: <regex>            # how to identify an affected form's deployed XML
            # or → script: <name> <args>   (when a regex can't express it)
**Fix:**    manual: <AI procedure>   # how to fix ONE form
            # or → script: <name> <args>   (deterministic, worth scripting)
**Verify:** must: <regex> ; must-not: <regex>   # optional auto-check (check_form.py) after the fix
**Max changed lines:** <N>                       # optional blast-radius guard (form_diff.py): flag forms whose fix touched more than N lines. Attribute-level fixes only — for additive/structural fixes set it to the expected per-instance insert size, or omit and rely on Verify / the fix's `ok`.
**Origin:** feedback-recurrence | engine-diff | manual
**Tracking:** #<n>                   # the consistent-problem GitHub issue
**Status:** open | piloted <YYYY-MM-DD> (N/TOTAL) | swept <YYYY-MM-DD>
```

**Verbs**
- `Detect: grep: <regex>` — the default; matched against each form's
  `.content.xml` by `find_affected.py`.
- `Fix: manual: <procedure>` — the default; the sweep agent edits each form's XML
  with judgment (extract → edit → repack), same discipline as the feedback Worker.
- `Fix: script: <name> <args>` — a deterministic fix. The script takes the form's
  ZIP, transforms its `.content.xml` in place, and prints a JSON report including a
  uniform **`ok`** boolean (form is now in the target state) that `run_sweep`'s
  verify consumes. Existing fixers to reuse: `swap_fragment` (fragRef value),
  `remove_attr` (drop an attribute), `set_attr_on` (ensure an attribute on
  selector-matched elements, quote-aware), `set_fragment_panel` (fragRef + panel
  attributes), `set_dor_template` (entity-aware DoR config). A new *shape* (e.g. a
  structural rewrite that synthesises nested nodes) needs a new such script.
- The whole staged run is executed by `run_sweep.py --slug PROBLEM-<x>
  [--pilot|--continue|--only]`, which reads this entry and does detect → fix →
  delta → diff → verify → deploy → issue row → one PR.

**Two traps in how this file is parsed — both fail silently (learned the hard way 2026-07-27):**
- **`Detect:` args must NOT be quoted, `Fix:` args MUST be.** `check_regressions.py::detector_argv()`
  splits the Detect verb with plain `.split()` and there is no shell, so `--select 'a="b"'` reaches the
  script *with* the quotes and matches nothing → **the rule reports 0 affected and looks green**.
  `apply_sweeps.py` / `run_sweep.py` split the Fix verb with `shlex.split()`, which strips quotes, so
  there they are required. (Cost: 258 affected forms reported clean, and #55 was unenforced for weeks.)
- **The on/off switch is `"swept" in Status` — a substring test on that one line.** So any wording that
  merely *mentions* the word enrols the rule in the guard, the intake app and `apply_sweeps`. Keep it
  out of the `Status:` line until the corpus really is done.

---

<!-- Problems are appended below this line. -->

## PROBLEM-banking-relationship-fragment — Banking-relationship panel: correct fragment + DoR exclusion
**Symptom:** the banking-relationship panel (the one bearing the BankingRelationship `fragRef`) needs two things the conversion engine emits: (1) the **one correct fragment** — `affrg_BankingRelationship1` (wrong forms use germany/italy/global variants or a dam-path reference; the correct one shows "UBS Europe SE" under Bankbeziehung), and (2) **`dorExclusion="true"`** so the whole panel is excluded from the Document of Record. The fix sets both on that panel; everything else on it (resourceType, name, dorExcludeTitle/Description) is left as-is.
**Detect:** script: `find_panel_noncanonical.py --match-frag BankingRelationship --exclude-frag CustodyAccount --require-frag /content/forms/af/afforms_ubs_fragmentlib/affrg_BankingRelationship1 --require-attr dorExclusion=true`  _(a form is affected if its banking panel lacks the canonical fragRef or `dorExclusion="true"`)_
**Fix:** script: `set_fragment_panel.py --match-frag BankingRelationship --exclude-frag CustodyAccount --set-frag /content/forms/af/afforms_ubs_fragmentlib/affrg_BankingRelationship1 --set-attr dorExclusion=true`
**Verify:** must: `fragRef="/content/forms/af/afforms_ubs_fragmentlib/affrg_BankingRelationship1"` ; must-not: `affrg_(germany|italiy|global)_BankingRelationship`  _(the per-panel `dorExclusion` is asserted by the fix's `all_canonical:true`, since a file-level grep can't tell which panel carries it)_
**Max changed lines:** 3   _(fragRef swap = 1 changed line, `dorExclusion` insert = +1 → up to 3 diff lines for a panel missing both; flag anything more, incl. the one multi-panel form)_
**Origin:** feedback-recurrence
**Tracking:** #10
**Status:** swept 2026-06-24 (212/212 fixed + deployed; 5 flagged diff>3, all verified benign — 4 had `dorExclusion="false"`→`true` plus the fragRef swap, AANB_019 has 3 banking panels). Open follow-up: whether the legacy custom UBS panel honors `dorExclusion` in the rendered DoR (the ~99 custom-panel forms may need the panel switched to the standard type for the exclusion to visually take effect; authored value is already correct).

**Scope note — summary exclusion deliberately NOT swept (2026-06-23):** the engine
*also* emits `summaryExclusion="true"` on this panel, but its summary-exclusion
output isn't correct yet, so we leave the deployed `summaryExclusion` exactly as-is
until the engine is fixed. This sweep enforces only the fragment + `dorExclusion`.

**Follow-up (2026-07-27):** the panel *containing* the banking fragment must also carry `css="ubs-margin-20"`. That panel is #29's `PN_BR` wrapper, so it lives in its own entry — [PROBLEM-banking-relationship-margin](#) — since this one is already done and `run_sweep` dispatches one Detect/Fix pair per slug. Nothing here changes.

**Review note:** "UBS Europe SE" appears legitimately all over these forms
(signatures, legal text, document titles) — do **not** auto-strip it. After the
swap, the per-form render-verify must inspect the deployed form for (a) a
**duplicate** "UBS Europe SE" under the banking section, and (b) **leftover inline
clearing/bank fields** (forms that had a custom-control banking panel). Flag ⚠️
for manual touch-up if either appears. `*_BankingRelationship_CustodyAccount` is a
different fragment — leave it alone (the detector/fix already exclude it).

## PROBLEM-dor-exclude-hidden — Uncheck "Exclude hidden fields from Document of Record"
**Symptom:** the DoR config (`dorProperties`) has `excludeFromDoRIfHidden="true"`, so hidden fields/sections are dropped from the Document of Record. It should be unchecked — the conversion engine omits the attribute entirely, and the 181 unchecked forms simply lack it.
**Detect:** grep: `excludeFromDoRIfHidden="true"`
**Fix:** script: `remove_attr.py --attr excludeFromDoRIfHidden`
**Verify:** must-not: `excludeFromDoRIfHidden=`
**Max changed lines:** 1   _(removing the attribute deletes exactly one line; flag any form whose diff touches more)_
**Origin:** feedback-recurrence
**Tracking:** #8
**Status:** swept 2026-06-23 (105/105, all checks passed)

## PROBLEM-jump-to-field-button — Jump-to-field button on the step-title panel + canonical title-node config
**Symptom:** the "Show jump to field button" option must be set on the **step-title panel** (`PN_FooTitle`) — in its **Summary** section — **not** on the title-draw. The engine wrongly emits `jumpToFieldButtonVisible="true"` on the `css="stepTitle"` title-draw (`panel.xml:55`), where it has no effect (**engine bug** — should move to the `panel_title` node). The old #12 sweep targeted the draw and was closed. The Summary section only exists on the UBS custom panel, so **[PROBLEM-panel-type-ubs](#) / #20 is a prerequisite** (merged 2026-07-01). Beyond jump-to-field this normalizes the title node to the shape the owner validated in AEM. FormConfigurator title panels keep their intentional DoR/summary exclusions (we only *add* on the panel, never strip).

Per step-title panel + its `headingLevel="2"` draw — **two cases**:

**Normal steps:**
- **panel** `PN_FooTitle`: add `jumpToFieldButtonVisible="true"` (791) + `dorExcludeDescription="true"`; **strip "rest to false"** — remove `summaryExclusion`/`dorExcludeTitle`/`dorExclusion` if present (7 panels).
- **draw** `TTL_Foo`: **remove** `jumpToFieldButtonVisible` (286) + add `summaryExclusion="true"` (210) + `dorExclusion="true"` (57).

**Form configurator** (the "Formular Adressat" config step — **not** real content; **only ever the first page**, so matched only on `step_idx == 0` to avoid false hits on later steps — detected by `formconfig`/`configurator` in the step / title-panel / **title-draw** name, or `css="ubs-margin-10"` on the step; **124 across the corpus**):
- **NO** `jumpToFieldButtonVisible` (config step gets no jump button — it would otherwise also show in the summary jump-list).
- **title panel** `PN_FooTitle` (the panel around the configurator title): add `dorExclusion="true"` (73) + `summaryExclusion="true"` (111) + `dorExcludeDescription="true"` → excluded from both the summary and the DoR. Never strip. (On the **panel**, like jump-to-field — not the step panel, not the draw.)
- **draw**: same as normal (strip jtf, add summaryExclusion + dorExclusion).
- First-page gate matters: without it, a later `PN_FormSection` (AAOF/AAOM/AAPR step 1) or a signature step with `css="ubs-margin-10"` (BAZA/BAZC step 4) would be misread as a configurator.

137 title nodes already correct; 4 steps have no title panel (skip). **251 forms affected.**
**Detect:** script: `find_jump_to_field.py`  _(per step-title panel/draw; emits `fix[]` {form,step,panel,deltas} + counts; affected = forms with any delta)_
**Fix:** script: `set_jump_to_field.py`  _(byte-offset, quote-aware open-tag scan for rich-text `_value`; adds the panel attrs, strips jtf from the draw, adds draw attrs; prints `{inner, changed:[…], ok}`)_
**Verify:** the fix script's `ok` (well-formed) + a re-scan showing 0 deltas remain + a pilot **visual spot-check** (jump-to-field button now renders on the step; title shows once).
**Max changed lines:** omit — mixed add/strip across two nodes per step; gate on the fix's `ok` + re-scan.
**Origin:** engine-diff (engine misplaces it on the draw; correct target is the panel)
**Tracking:** #22 (supersedes closed #12)
**Status:** **swept 2026-07-02 (254/254 fixed + deployed, PR #26 merged)** — jump-to-field on the step-title panel; canonical title-node config; form-configurator (first-page) gets no button + title panel excluded from summary+DoR. Engine still misplaces jtf on the draw (upstream fix pending).

## PROBLEM-dor-custom-template — DoR config: custom template (metaTemplateRef) + "Use Adaptive Form Title"
Two DoR-config fixes the engine emits, applied together (all **other** master-page properties are left exactly as-is, per request 2026-06-24):
1. **Custom template** — `metaTemplateRef` on `dorProperties` should be the entity-General template, not the generic Blank. Engine branches on entity (config.toml): 019 → `…/02_forms/UBS_General_Germany_DOR.xdp`, 033 → `UBS_General_Italy_DOR.xdp` (else, e.g. CH 001 → `UBS_Blank_DoR.xdp`, so Blank is correct there). 93 forms have Blank.
2. **Form Title = "Use Adaptive Form Title"** — the Header `AF_FORM_TITLE` node should be `valueFrom="formTitle"` with **no** `value` and **no** `fd:translationIds` (the engine's exact output). 31 forms deviate (29 hold a hardcoded `value` with `valueFrom="     "`, 2 use `valueFrom="template"`). The 44 forms with **no** `AF_FORM_TITLE` node (minimal master-page) are skipped — we don't synthesize the node ("leave properties as they are").

**Combined affected:** 121 forms (90 template-only, 28 form-title-only, 3 both; 165 already correct).
**Detect:** script: `find_dor_config.py` _(to build for --continue: affected if `metaTemplateRef` basename is `UBS_Blank_DoR.xdp` on a 019/033 form, OR `AF_FORM_TITLE` is present and not already `valueFrom="formTitle"` with no value/translationIds)_
**Fix:** script: `set_dor_template.py --replace-basename UBS_Blank_DoR.xdp --use-adaptive-form-title`  _(entity from JCR path; metaTemplateRef only changes Blank→General; `--use-adaptive-form-title` sets AF_FORM_TITLE valueFrom=formTitle and drops value+translationIds; no-ops anything already correct)_
**Verify:** must: `metaTemplateRef="[^"]*UBS_General_(Germany|Italy)_DOR\.xdp"` ; must-not: `UBS_Blank_DoR\.xdp` — **plus** the AF_FORM_TITLE node must be `valueFrom="formTitle"` with no value/translationIds (checked directly, since a file-level grep can't isolate one node). The fix reports `changed` + `form_title_changed`.
**Max changed lines:** 6   _(metaTemplate replace = 2; AF_FORM_TITLE: drop value + drop translationIds + valueFrom replace ≈ up to 4)_
**Origin:** engine-diff
**Tracking:** #14
**Status:** swept 2026-06-24 (121/121 — combined fix; AAXC_019 + BAAD_019 had only the Form Title set, their out-of-scope Custody/Blank-BankingRelationship templates correctly left untouched). Verify-check note: the `must=General` template assertion false-flags form-title-only forms whose template is a non-General variant — tighten to "template==General OR was not Blank" if this sweep is re-run.

**Scope note — only `UBS_Blank_DoR.xdp` (custom template) + the Form Title property are swept.** All other master-page properties (`ShowBankingRelationship`, `senderAddressTitle`, `APPCode`, structural nodes, `AF_HEADER_TEXT`, etc.) are **left untouched** per the 2026-06-24 decision. Other non-General templates (`UBS_Blank_Letter_DoR.xdp` 44, `UBS_Custody_<entity>` 8, `UBS_Blank_BankingRelationship` 2) are also left as-is.

**Rollout note — overlaps other sweeps.** ~90% of these 93 forms are also in the banking (#10) and jump-to-field (#12) affected sets — **0 are free of both**. Separate sweep branches that edit the same `_merged.zip` conflict at merge (binary/LFS, no 3-way merge). **Sequence the rollouts**: fully merge one sweep, rebase the others on the new master, then `--continue` (all three fixes are idempotent, so re-running on the merged base is safe). Pilots are chosen to avoid forms already committed to another sweep branch, so no conflict arises before rollout.

## PROBLEM-step-title-panel — Each wizard step's title wrapped in a step-title panel
**Symptom:** the engine wraps every wizard **step**'s title in a dedicated *step-title panel* (`guidePanel` named `{step}Title`, holding a `headingLevel="2"` title-draw) as the step's **first child** (engine `profiles/ubs/aem/panel.xml`; **engine confirmed authoritative for this problem** by the owner — *but only for the wrap structure; the engine does NOT reliably detect the title text, so title text comes from the deployed form, never the engine*). A *step* = a panel directly under `guideContainer › rootPanel(guideRootPanel) › items`, excluding the specials (`summary`/`preview`/`formmetadata`/`signerInfo`/…). Per-step verdict (the agreed rule — title text taken **verbatim** from the form, this sweep only changes *structure*):
- **ok** (609) — already has a dedicated step-title panel, **or a child draw already renders the title** (a `titledraw`/`textdraw` whose `_value` text *exactly* equals the step title) → **skip** (synthesizing would duplicate the visible heading). **Only a drawn `_value` counts as "rendered" — a panel's `jcr:title` does NOT render as a visible heading** (validated on AAAI `PN_Agreement`, AACB `PN_Parte1`, `PN_StpSectionIiSectionTitle` — all panels whose `jcr:title` matched the step title but showed no heading; those steps need synth, not skip).
- **wrap:move** (77) — a `headingLevel="2"` draw exists but isn't wrapped → **move that draw intact** to the top of the step and wrap it; remove the step's own `jcr:title`. Translation-safe (validated: AAAM move kept its German title).
- **wrap:synth** (274) — no draw renders the title → synthesize a draw from the step's `jcr:title` at the top; remove the step's `jcr:title`. Covers steps whose title only lived in a panel `jcr:title` (AAAI, AACB sections, AAAL — all validated single after).
- **strip:double** (26) — already wrapped (a `headingLevel="2"` draw whose text *equals* the step's own `jcr:title`) **and** the step still carries that `jcr:title` → AEM renders the heading **twice on screen** (`dorExcludeTitle` only hides it in the DoR/PDF, not the form) → **strip the redundant step `jcr:title`** (no insert). Validated: AACB first two showed twice, single after.
- **wrap:promote** (45) — no h2 draw and no step `jcr:title`, **but the step LEADS with a `titledraw`** (its real title rendered at the wrong heading level: `h3`/`h4`/none) → promote that draw to a proper step title: set `headingLevel="2"` + `css="stepTitle"`. **If it's already inside a dedicated step-title panel** (17 of 45) → promote **in place** (no new panel — else double-wrap); **otherwise** (28 of 45) → move it intact to the top and wrap it in `{name}Title`. A leading *textdraw* (body text) or no draw → stays titleless. Validated: AACT (in-place, single panel), AAEU "Checkliste"/AAFA "Finanzdienstleister" (move).
- **flag:trans** (7) — `jcr:title` is translation-linked (path-keyed) and no draw renders it → auto-wrap + flag "update translations".
- **titleless** (31) — no title anywhere (no h2 draw, no `jcr:title`, and the leading drawn element is a textdraw/body or absent) → **skip**.

Rules that made the fix safe (all from AEM validation): **remove the step's own `jcr:title`** when wrapping (else AEM renders it a second time), **a panel `jcr:title` never renders a visible heading** (so only a matching drawn `_value` blocks a synth — everything else gets a proper title), and **strip the step `jcr:title` when a wrapped draw already shows the same text** (`strip:double`). Translations are text-keyed (`dictionary/<lang>.xml`, `sling:key="fd_<text>"`), so keeping title text verbatim preserves them.

Detection keys on a `headingLevel="2"` title-draw, **not** `css="stepTitle"` (that marker also sits on wrapper panels / `textdraw`s and is absent on ~415 — it both over- and under-matches). **182 forms / 422 auto-fixes (77 move + 274 synth + 26 strip + 45 promote) / 7 flags** of 286 packages.
**Detect:** script: `find_step_title_panel.py`  _(emits `wrap[]` {form,step,source:move|synth|strip|promote,title} and `flag[]` {…,reason}; that is the review surface — verify it before building/running the fix)_
**Fix:** script: `set_step_title_panel.py` _(byte-offset structural surgery: insert a `{name}Title` panel as the step's first child; `move` = relocate the existing draw intact (preserves translations), `synth` = build a draw from `jcr:title`, `strip` = remove the redundant step `jcr:title` (no insert), `promote` = a leading titledraw at the wrong heading level → set `headingLevel=2`+`css=stepTitle` (in place if already in a title panel, else move+wrap); all also remove the step's own `jcr:title` and set `dorExcludeTitle="true"` to match the engine. Auto-fixes `move`+`synth`+`strip`+`promote`+`flag:trans`; skips `ok` and `titleless`. Mirrors the detector's skip rules. Prints `{inner, wrapped:[{step,source}], ok}`.)_
**Verify:** the fix script's per-step `ok` (well-formed result + every targeted step now has a first-child step-title panel, or had its redundant title stripped) + a pilot **visual spot-check** (title shows once, correctly). The 2 `flag:trans` steps are auto-wrapped too but **listed in the issue table as "update translations"** — the owner tops up their translations after rollout.
**Max changed lines:** omit — additive structural insert; gate on removed==0 (move adds/relocates, never deletes content) + the fix's per-step `ok`.
**Origin:** manual (engine-confirmed for structure only)
**Tracking:** #18
**Status:** **swept 2026-06-30 (182/182 fixed + deployed, PR #19 merged).** Detector + fixer validated in AEM on AAAM (move, translation held), AAAN (synth), AACB (strip:double + 26 section synths), AAAI/AAAL (synth, no double), AACT/AAEU/AAFA (promote). Three rule corrections during the pilot: added `strip:double` (AACB's first two doubled); tightened `renders_title_elsewhere` to **draw `_value` only** (a panel `jcr:title` doesn't render → titles living only in a panel `jcr:title` are now synthed); added `wrap:promote` (titleless step leading with a titledraw at the wrong heading level → promote to h2, in place if already in a title panel else move+wrap). The 7 `flag:trans` steps (4 forms) were resolved **not-actionable** — all are `jcr:language="en"` with orphaned dictionaries (no live second locale), so the English synth titles are correct. 31 steps remain genuinely titleless. One edge: `contentPanel_forInternalBankUseOnly` (1 form) is a direct root child that looks like content, not a step.

**Dependency:** this is the **prerequisite** the jump-to-field sweep (#12) is deferred behind — jump-to-field's button goes on the step-title draw, which must first be correctly wrapped. **Land #18 before re-piloting #12.** Also overlaps banking (#10) / dor-template (#14) on the same `_merged.zip`s → sequence rollouts (merge one, rebase + `--continue` the rest; the fix re-derives each zip from base, so idempotent).

## PROBLEM-panel-type-ubs — Panels should use the UBS custom panel component, not the default AEM panel
**Symptom:** wizard panels use the **default AEM panel** (`sling:resourceType="fd/af/components/panel"`) instead of the **UBS custom panel** (`ajila-forms-customers/ajila-forms-ubs/components/controls/panel`). The default panel lacks the UBS authoring sections (notably the **Summary** tab that carries `jumpToFieldButtonVisible`), so those options can't be set/rendered. **Every non-fragment panel must be the UBS custom panel** — including repeatable **wrappers** (owner directive 2026-07-20; see revert note below). **Fragment panels (`fragRef`) are excluded** — they legitimately use the default panel (owned by [PROBLEM-fragment-panel-aem](#) / #24).
**Detect:** script: `find_panel_type.py`  _(counts/lists `guideNodeClass="guidePanel"` nodes with `sling:resourceType="fd/af/components/panel"` and no `fragRef`; `affected` = forms with ≥1)_
**Fix:** script: `set_panel_type.py` _(byte-offset splice: change only the `sling:resourceType` value `fd/af/components/panel` → `ajila-forms-customers/ajila-forms-ubs/components/controls/panel` on qualifying guidePanels; fragments (`fragRef`) and already-UBS panels untouched; prints `{inner, swapped, skipped_fragment, ok}`)_
**Verify:** the fix script's `ok` (well-formed) + a re-scan showing 0 non-fragment default guidePanels remain + a pilot **visual spot-check** (panels render identically on the UBS component). Fragments must stay on the default panel.
**Max changed lines:** omit — one attribute value changed per swapped panel (no adds/removes); gate on the fix's `ok` + re-scan.
**Origin:** engine-diff (engine emits the UBS custom panel; deployed forms + our step-title wrappers used the default) — but see revert: the owner's rule is blanket-UBS for all non-fragment panels, which **overrides** the engine's `repeatable.xml` (it emits the repeatable *outer wrapper* as the default AEM panel; the owner does not want that in the deployed forms).
**Tracking:** #20
**Status:** **swept 2026-07-01 (258/258 fixed + deployed, PR #21 merged)** — 2,798 non-fragment default-AEM panels swapped to the UBS custom panel; 666 fragment panels left untouched (verified byte-identical on AACR: 11 default + 4 UBS fragments). Prerequisite for #12 (jump-to-field) — now unblocked. **Bidirectional experiment reverted 2026-07-20:** a rerun (PR #62, since reverted) had made the detector/fixer bidirectional and flipped 310 non-fragment repeatable *wrappers* from UBS → default AEM panel to match the engine's `repeatable.xml`. On AEM review the owner directed that **all non-fragment panels stay UBS** (repeatable wrappers included), so the scripts were restored to the one-way `default → UBS` form and the 310 wrappers across 161 forms were swapped back to UBS and **redeployed (PR #63, 2026-07-20 — 161/161, 0 flagged, `verify:ok`+`deploy:ok`)**. Fragment repeatables are still handled by #24 (fragment panel → default AEM); only non-`fragRef` panels are forced to UBS here.

## PROBLEM-fragment-panel-aem — Fragment panels should use the default AEM panel, not the UBS custom panel
**Symptom:** fragment-reference panels (`guideNodeClass="guidePanel"` with a `fragRef`) use the UBS custom panel (`ajila-forms-customers/ajila-forms-ubs/components/controls/panel`) instead of the **default AEM panel** (`fd/af/components/panel`) the engine emits for fragments. **Inverse of [PROBLEM-panel-type-ubs](#) / #20** (which swaps *non-fragment* panels default→UBS and leaves fragments alone). Corpus: **629** UBS-type fragment panels across **202 forms** (666 already default). Includes the hidden `formmetadata` fragment.
**Detect:** script: `find_fragment_aem.py`  _(counts fragRef guidePanels by resourceType; affected = forms with ≥1 UBS-type fragment panel)_
**Fix:** script: `set_fragment_aem.py` _(byte-offset splice: change only `sling:resourceType` UBS→`fd/af/components/panel` on fragRef guidePanels; non-fragment panels + already-default fragments untouched; prints `{inner, swapped, ok}`)_
**Verify:** the fix's `ok` (well-formed) + a re-scan showing 0 UBS-type fragment panels remain + a pilot visual spot-check (fragments render identically on the default panel).
**Max changed lines:** omit — one attribute value per swapped fragment panel; gate on `ok` + re-scan.
**Origin:** engine-diff (engine emits fragment panels as the default AEM panel)
**Tracking:** #24
**Status:** **swept 2026-07-02 (202/202 fixed + deployed, PR #25 merged)** — 629 UBS fragment panels → default AEM; already-default fragments + non-fragment panels untouched. Engine-confirmed (fresh AAAE conversion emits fragment panels as `fd/af/components/panel`). `formmetadata` fragment included (engine emits a metadata *control*, not a fragment, so no engine ref — treated as a normal fragRef panel).

## PROBLEM-nav-button-order — Wizard toolbar navigation buttons in the wrong order
**Symptom:** the toolbar's core nav buttons must be in the engine order `nextitemnav` (Next) → `submit` (Submit) → `previtemnav` (Back), with any extra buttons (preview / `fwbSaveProgress` / custom preview / tertiary) after. 66 forms deviate — mostly Back-first (`previtemnav, nextitemnav, submit`), 4 Submit-first. Engine-confirmed via fresh conversions (AAAN, AAAE): order is Next, Submit, Back, Preview.
**Detect:** script: `find_nav_order.py`  _(compares the relative order of the three core buttons in the toolbar; affected if all three present but not Next→Submit→Back)_
**Fix:** script: `set_nav_order.py` _(byte-offset element relocation: moves the three core buttons to the front in engine order, keeps every other toolbar child in its existing relative order; prints `{order_before, order_after, ok}`)_
**Verify:** the fix's `ok` (well-formed) + re-scan showing 0 wrong + a pilot visual check (button row renders in the right order).
**Max changed lines:** omit — structural reorder; gate on `ok` + re-scan.
**Origin:** engine-diff (engine emits Next, Submit, Back, Preview)
**Tracking:** #27
**Status:** **swept 2026-07-02 (66/66 fixed + deployed, PR #28 merged)** — toolbar reordered to engine order Next→Submit→Back, trailing buttons kept.

## PROBLEM-banking-relationship-wrap — Wrap the first-page banking panel in a PN_BR exclusion panel
**Symptom:** the **first-page** banking-relationship fragment panel (`guideNodeClass="guidePanel"` with a `fragRef` ending in `affrg_BankingRelationship1`) must be wrapped in a dedicated UBS custom panel named `PN_BR` carrying `summaryExclusion="true"` + `dorExclusion="true"`, so the whole banking subtree is excluded from the summary and the DoR. This is an established hand-authored convention — **21 forms already ship the `PN_BR` wrapper** (UBS custom panel, both exclusions, banking fragment as sole child). The fragment panel already carries `dorExclusion="true"` from [PROBLEM-banking-relationship-fragment](#) / #10; this sweep adds the *summary* exclusion via the wrapper and gives the DoR exclusion its correct structural home (a wrapper isolates the whole subtree, which flags on the fragment panel alone may not do in the rendered summary). **First page only** — banking panels on later wizard steps are left alone (9 forms are later-page-only; no form has banking on both page 1 and a later page).
**Detect:** script: `find_banking_wrap.py`  _(finds first-page banking fragRef panels; affected = forms with ≥1 unwrapped one; already-`PN_BR` (name, or a UBS panel with both exclusions + sole child) and later-page banking are skipped — idempotent for rebase + --continue)_
**Fix:** script: `set_banking_wrap.py`  _(byte-offset wrap surgery mirroring #18: moves the fragment panel **intact** into a new `PN_BR` UBS custom panel with summaryExclusion+dorExclusion; multiple panels spliced back-to-front; prints `{inner, wrapped, skipped_wrapped, skipped_later, ok}`)_
**Verify:** the fix's `ok` (well-formed) + a re-scan showing 0 unwrapped first-page banking panels remain + a pilot **visual spot-check** (banking section no longer appears in the summary; the form itself renders unchanged).
**Max changed lines:** omit — additive structural wrap; gate on the fix's `ok` + re-scan.
**Origin:** manual (candidate engine-fix — the engine emits the exclusion flags *on* the fragment panel in `profiles/ubs/aem/fragment.xml`, which may not exclude the rendered subtree from the summary; the `PN_BR` wrapper is the reliable structure the owner hand-authored on 21 forms).
**Tracking:** #29
**Status:** **swept 2026-07-02 (232/232 fixed + deployed, PR #30 merged)** — first-page banking fragment wrapped in a `PN_BR` UBS panel with summaryExclusion+dorExclusion; 22 already had `PN_BR`, 9 later-page-only left untouched. `validate_delta` was updated to ignore benign ancestor re-paths (a wrap/move re-paths a pre-existing violation deeper — one-in/one-out, net-zero), so the rollout ran 0-flagged.

**Downstream:** the `PN_BR` wrapper this sweep creates must also carry `css="ubs-margin-20"` → [PROBLEM-banking-relationship-margin](#). `set_banking_wrap.py` emits no `css`, so every wrapper it made is affected.

## PROBLEM-nav-save-progress-button — Remove the "Save Progress" button from the wizard toolbar
**Symptom:** the wizard toolbar carries an extra **"Save Progress"** button — a `guidebutton` (`sling:resourceType="fd/af/components/guidebutton"`, `guideNodeClass="guideButton"`) named `fwbSaveProgress`, a direct child of the toolbar's `items` (each with `fd:scripts` + `fd:rules` children). It should be removed from every form. **119 of 286 packages** carry it. Shape is **uniform** across the corpus (no lineage split): exactly one `fwbSaveProgress` guidebutton per affected form, always a direct child of `toolbar > items`.
**Detect:** script: `find_save_progress.py`  _(affected = forms whose toolbar `items` has a child named `fwbSaveProgress`; matches on the name in the toolbar, not a raw substring, so a stray reference elsewhere wouldn't false-positive — idempotent: a fixed form reports 0)_
**Fix:** script: `remove_save_progress.py`  _(byte-offset element excision of the `fwbSaveProgress` guidebutton, incl. its preceding indent so no dangling blank line; prints `{inner, removed, ok}`)_
**Verify:** must-not: `name="fwbSaveProgress"`  _(+ the fix's `ok` (well-formed) + a re-scan showing 0 affected + a pilot **visual check**: the toolbar no longer shows a Save Progress button and the button row renders unchanged otherwise)_
**Max changed lines:** omit — structural deletion; gate on the fix's `ok` + re-scan + must-not.
**Origin:** manual (owner directive — remove the Save Progress button everywhere)
**Tracking:** #50
**Status:** swept 2026-07-20 (119/119 fixed + deployed, PR #51) — pilot (6) reviewed OK, then --continue for the remaining 112, all 0-flagged. delta:skip throughout (engine MCP not built; pure well-formed-subtree deletion — no new violations possible). AABJ_019 was deferred behind feedback PR #49 (LFS conflict on the shared `_merged.zip`) and swept once #49 landed. Marked swept 2026-07-20 after `check_regressions.py` confirmed the detector reports 0 affected on master.

## PROBLEM-footnote-modernize — Footnotes → canonical engine (AAEP) model
**Symptom:** footnotes must use the engine/AAEP model — an inline reference `<span data-af-footnote-id="ID"><sup>#</sup></span>` in the rich value, a hidden description `<p id="ID" class="footnoteDescription" style="display: none;"><span class="footnoteDescText">TEXT</span></p>` right after it, and one `guidefootnoteplaceholder` (`guideNodeClass="guideFootnotePlaceHolder"`) per page where footnotes render. Many deployed forms instead use a **legacy accordion** (`css="ubsAccordionFootnote"` panel holding the footnote texts, with raw `<sup>N</sup>` / unicode `¹²³` markers in rich `_value`s **and** choice `options` labels), or carry modern refs but are **missing the placeholder**, or have a **stray placeholder** with no refs. **35 of 286 packages** are safely automatable (32 accordion + 2 add-placeholder + 1 stray). 7 more need per-form judgment and are **out of scope** (accordion holding non-footnote prose: AAPC/AAXC; footnote text with no reference: AAPQ; orphan markers with no recoverable text: AACY/AAFC/AAQG/ABFG).
**Detect:** script: `find_footnote_noncanonical.py`  _(returns only auto-safe forms: accordion forms where **every** body marker — sup or unicode, in `_value` or `options` — resolves to a footnote text extractable from the accordion, plus modern-refs-missing-placeholder and stray-placeholder; excludes misused/unresolved/orphan as manual. Idempotent: a converted form matches none of the shapes.)_
**Fix:** script: `set_footnote_modern.py`  _(escaped-domain byte-offset surgery: extract marker→text from each accordion, rewrite every marker to inline ref + hidden desc (in `_value`s and per-option `options` labels, setting `textIsRich`), replace each accordion **in place** with a placeholder; add/remove placeholder for the other two modes. Ports the engine's ID scheme `base64("0."+marker)`−`=+/` and exact span/desc strings. Prints `{mode, footnotes, refs, placeholders, ok, flags}`; **hard self-check** sets `ok:false` on any leftover resolvable marker, remaining accordion, or malformed output.)_
**Verify:** must-not: `ubsAccordionFootnote`  _(+ the fix's `ok` (well-formed, no leftover marker, accordion gone, ref produced) + a pilot **visual check**: footnote superscripts render as `#` linking to the description, the placeholder shows the footnotes, and option-label footnotes render as rich text not literal markup)_
**Max changed lines:** omit — structural rewrite of varying size; gate on the fix's `ok` + must-not + delta.
**Origin:** manual (owner directive — engine `profiles/ubs/aem/footnote*.xml` + `converter.rs` is the confirmed-correct reference; deployed exemplars AAEP and AACR/BATI)
**Tracking:** #53
**Status:** swept 2026-07-20 (35/35 automatable fixed + deployed, PR #54) — pilot (6, all shapes incl. unicode-in-options + rich lists) reviewed OK, then --continue for the remaining 29; all 0-flagged with delta passing (engine MCP: no new schema violations). **7 forms deliberately out of scope** (manual): AAPC/AAXC (accordion holds non-footnote prose), AAPQ (footnote text, no reference), AACY/AAFC/AAQG/ABFG (orphan markers) — these remain by design; the detector returns only auto-safe shapes, so it reports 0 on master. Marked swept 2026-07-20 after `check_regressions.py` confirmed 0 affected.

## PROBLEM-metadata-name — Metadata textdraw control: name should be `metadataTextDraw`, not `metadata`
**Symptom:** the hidden metadata text-draw control (`sling:resourceType="ajila-forms-customers/ajila-forms-ubs/components/controls/metadata"`, `guideNodeClass="guideTextDraw"`, `_value="Metadata"`, `visible="false"`, `dorExclusion="true"`) carries the AF element `name="metadata"` on some forms. The element **name** (the `name=` attribute, **not** the JCR node — the tag stays `<metadata>`) should be **`metadataTextDraw`**. Forms where the engine auto-generated a timestamped name (`name="metadata1718013683061"`) are **correct** and left untouched — only the exact value `metadata` is changed ("if it is, change it"). The corpus is two lineages, so a single form can carry **two** metadata controls (one legacy `name="metadata"`, one engine `name="metadata<ts>"`); only the first is touched. **222 of 286 packages affected** (227 occurrences); `name="metadata"` (exact) appears **only** on this control (0 false positives elsewhere), and nothing references it by name (the `var metadata = window.forms.ubs.getFormMetadata()` scripts + `metadataselector` attribute are unrelated), so the rename is a safe self-contained attribute edit. Changing only the attribute (not the node) keeps `fd:translationIds` `…##metadata##…` paths valid.
**Detect:** script: `find_attr_value.py --select controls/metadata --attr name --value metadata`  _(quotes removed 2026-07-27: with them, `detector_argv`'s `.split()` passed them through literally and this rule silently reported 0 affected — it was never actually enforced. Corpus verified clean either way.)_  _(affected = forms with a `controls/metadata` open tag whose `name` equals exactly `metadata`; exact match, so `metadata<ts>` is not flagged — idempotent: a fixed form reports 0)_
**Fix:** script: `set_attr_value.py --select 'controls/metadata' --attr name --from metadata --to metadataTextDraw`  _(quote-aware open-tag rewrite: on `controls/metadata` tags, change `name="metadata"` → `name="metadataTextDraw"`; timestamped siblings untouched; prints `{inner, matched, changed, remaining, ok}` — `ok` = no `name="metadata"` remains on a metadata control; idempotent)_
**Verify:** must-not: `name="metadata"`  _(+ the fix's `ok`; a plain must-not is exact — it does not match `name="metadata1718013683061"`)_
**Max changed lines:** 4   _(one attribute value changed per affected control; form_diff counts an in-place line edit as remove+add = 2. Five _033 forms carry **two** `name="metadata"` metadata controls → 4 diff lines; the bound covers them. A single-control form should be 2.)_
**Origin:** manual (owner directive — the metadata textdraw element name should be `metadataTextDraw`)
**Tracking:** #55
**Status:** swept 2026-07-20 (222/222 fixed + deployed, PR #56) — pilot of 6 (AAAQ/AABH/AABN/AABO_019, AAEA/AAGZ_033) reviewed OK, then `--continue` for the remaining 216; every form `verify:ok` + `deploy:ok`, `delta:skip` throughout (engine MCP not built — a pure attribute-value edit can't introduce schema violations). **5 forms flagged `diff` only, all verified benign:** AAOF/AAOM/AAQD/AAQG/BANW_033 each carry **two** `name="metadata"` metadata controls (distinct panels), so both were renamed → 4 diff lines (bound raised 1→2→4 accordingly). AAEA_033 (one legacy + one engine-timestamped control) had only the legacy `name="metadata"` renamed, the `name="metadata1742297849078"` sibling left intact — confirming exact-value matching. Only the `name=` attribute changed (node tag stays `<metadata>`, `##metadata##` translationIds intact). Marked swept 2026-07-20 after PR #56 merged and `check_regressions.py` confirmed the detector reports 0 affected on master.

## PROBLEM-formcode-page-title — Form-root page title must be the bare form code
**Symptom:** the form-root page title — `jcr:title` on the `cq:PageContent` node of the form's `aftemplatedpage` (`sling:resourceType=".../pages/aftemplatedpage"`) — must equal the **bare form code** (engine `root.xml:16`: `jcr:title="{{ form_code }}"`, **engine-confirmed authoritative for this property** by the owner). The code is always present in form data as `formrange_code` on the hidden metadata control (single .content.xml holds both the page node and the form body). **113 of 286 packages deviate:** 109 carry the machine default `AF_<code>`, 4 carry a descriptive human title (AAAV/AACE/AACS/AACW) — **all overwritten with the bare code** per the owner's "always the code" rule (decision A, 2026-07-20). The 173 already-bare-code forms are left as-is. No form carries an `AF_`-prefixed `jcr:title` on any non-page node, so the fix's page-node target and the `must-not` guard cannot collide with panel/step titles.
**Detect:** script: `find_pagetitle_code.py`  _(affected = the `cq:PageContent` node's `jcr:title` != the form's `formrange_code`; emits `detail[form]={code,title}`. Exact compare, so bare-code forms report 0 — idempotent)_
**Fix:** script: `set_pagetitle_code.py`  _(quote-aware open-tag rewrite: on the `cq:PageContent` open tag, set `jcr:title` to the per-form `formrange_code` (derived from the form, no `--from/--to`); every other `jcr:title` untouched; prints `{inner, code, before, changed, ok}` — `ok` = the page node now carries `jcr:title="<code>"`; idempotent)_
**Verify:** the fix's `ok` (page title == form code) + a re-scan showing 0 affected ; must-not: `jcr:title="AF_[A-Z0-9]{3,6}"`  _(safe: `AF_` titles occur only on the page node; a per-form `must` can't be expressed corpus-wide, so `ok` is the real check)_
**Max changed lines:** 2   _(one in-place value edit = remove+add = 2 diff lines; flag anything more)_
**Origin:** engine-diff (engine `root.xml:16` emits `jcr:title="{{ form_code }}"`)
**Tracking:** #59
**Status:** swept 2026-07-20 (113/113 fixed + deployed, PR #60) — pilot of 6 (AAAB/AAAE/AAAI/AAAL/AAAM/AAAQ_019) reviewed OK, then `--continue` for the remaining 107; every form `verify:ok`+`deploy:ok`, 0 flagged, `delta:skip` throughout (no engine MCP — a lone `jcr:title` value edit can't introduce schema violations). The 4 descriptive-title forms (AAAV/AACE/AACS/AACW_019) were overwritten with the bare code per decision A and deploy-verified on AEM (e.g. AACW "Cost information for investments…" → `AACW`). Marked swept 2026-07-20 after PR #60 merged and `check_regressions.py` confirmed the detector reports 0 affected on master.

## PROBLEM-naming-conventions — Component names must carry the correct type `PREFIX_`
**Symptom:** every author-named component's **`name` attribute** (not the JCR node tag — tags are AEM-internal ids like `panel_77897ce3…` and never conform) should follow `PREFIX_<CamelCaseName>_<shortUuid>` with `PREFIX` fixed per component type. **Owner-relaxed rule (2026-07-21): only the leading `PREFIX_` is enforced** — the `<shortUuid>` is **optional** and the `<CamelCaseName>` is **left exactly as-is (never invented)**. So the only defect is a name whose leading token is not the correct prefix for its type: legacy/lowercase repeat prefixes (`CP_…`, `RP_…`, `panel_…` → `RCP_`/`PN_`), titledraws named `ST_…` (→ `TTL_`), signatures named `TXT_…` (→ `SIGN_`), numericboxes named `NUM_…` (→ `NB_`), `guidedatepicker` named `DP_…` (→ `DATE_`), case slips (`Date_…`, `Pn_…`), timestamped auto-names (`textdraw1780315242122` → `ST_…`). **260 / 286 packages affected, 2,177 violations** (`wrong-prefix` 835 · `raw` 1,342; 1,892 high-confidence, 285 low). Names already carrying the right prefix are **ok** regardless of a missing uuid/CamelCase (`BT_Remove`, `PN_BankingRelationship`, `PN_958d2a4a` all pass).
  **Authoritative prefixes (owner, 2026-07-21) — the ONLY allowed leading tokens; any other must convert to one of these:** `RB_` `DATE_` `TXT_` `CB_` `DD_` `PN_` `RCHT_` `RCP_` `RCBP_` `RCHP_` `NB_` `BT_` `ST_` `TEL_` `TTL_` `TXTM_` `EML_` `IMG_` `CRT_` `SPT_` `ITXT_` `ETXT_` `TBL_` `BARCODE_` `QRCODE_`. Type→prefix: textbox/guidetextbox→TXT, checkbox→CB, radiobutton→RB, dropdownlist→DD, datepicker **and guidedatepicker**→DATE, numericbox→NB, telephone→TEL, email→EML, textboxMultiline→TXTM, titledraw→TTL, textdraw→ST (ITXT/ETXT also accepted), *button→BT.
  **Reconciled scope (owner decisions — do not silently change):**
  - **`SIGN_` is an owner-allowed exception** for `signature` components ("allow SIGN for signatures for now") — not in the authoritative table, but valid; a `TXT_`-named signature converts to `SIGN_`. `DP_` is **not** allowed (guidedatepicker→`DATE_`).
  - **Exempt** system/fixed-name components (guideContainer, rootPanel, toolbar, submit, next/previtemnav, summary, dorOptionsUBS, metadata, letterhead, carousel\*, messagebox\*, previewbutton, signaturebox, footnote placeholders, layouts), the UBS **"Preview"** panel+button (`name="preview"`, referenced by the preview feature), and `{name}Title` wrapper panels (engine artifacts tracking their parent).
  - **Exempt fragment-referenced fields**: any node with a `fragRef`, an `affrg`-containing name, or an `AF_`-prefixed name (renaming breaks the fragment binding contract).
  - **Repeat panels** (a panel with `minOccur`/`maxOccur` or a `repeatable_*` tag) expect the `RCP_` family; a non-repeat panel carrying `RCP_` is flagged back to `PN_`.
**Detect:** script: `find_naming_violations.py`  _(walks the whole form tree; a name is `ok` iff its leading token is a valid prefix for the node's type/role, else `wrong-prefix` (leading token is a canonical prefix, wrong type) or `raw` (no canonical prefix). Each carries a `confidence`: high for type→prefix + clear panel roles; low for repeat-container sub-roles that need human eyes. Affected = ≥1 non-ok, non-exempt node. `--form AABI_019|AF_AABI` for one form; `--verbose`/`--limit` for the review surface. Idempotent: a fixed form reports 0.)_
**Fix:** script: `set_naming_prefix.py`  _(deterministic prefix normalisation — for each flagged node it (1) strips a leading lowercased resourceType word (auto-names like `tertiarybutton1597…`, `panel_Foo`), else (2) swaps a known/legacy leading prefix (`CP_`/`RP_`/`NUM_`/`PN_`-on-repeat/`ST_`-on-titledraw → the canonical prefix), else (3) prepends the prefix to a real name word (`Client_Advisor`→`TXT_Client_Advisor`) — **keeping the rest of the name verbatim**. It then rewrites **every reference** to the old name in one boundary-safe whole-identifier pass (`.NAME`, `(NAME`, `NAME.instanceManager`, slash-paths, `##NAME##` translation ids), skipping XML tag positions. **High-confidence only by default**; low-confidence repeat-container sub-panels are reported under `flagged_low` and left for a manual pass (`--include-low` to also rename them). Collisions (new name == a surviving name, or two names colliding, or a duplicate name classified to >1 prefix) are reported under `collisions` and skipped — never merges two fields. Prints `{renamed, flagged_low, collisions, remaining_high, ok}`; `ok` = no collisions AND 0 high-confidence violations remain.)_
**Verify:** the fix's own `ok` (no collisions + 0 high-confidence violations remain) is the primary check; re-run `find_naming_violations.py --form <F>` to confirm only `flagged_low` (if any) remain. The `delta` gate must stay green (a broken reference would surface as a new schema violation). **No `Max changed lines` bound** — renaming rewrites every reference, so the diff is intentionally large.
**Origin:** manual (owner directive — the attached *AEM Naming Conventions* doc)
**Tracking:** #64
**Status:** swept 2026-07-21 (260/260 addressed — 252 fully conformant + 8 partial-fixed, one accepted shared-name node each; PR #65) — pilot of 6 (AAAA/AAAB/AAAE/AAAI/AAAL/AAAM_019) reviewed (found + fixed the escape-boundary orphan bug), then `--continue` deployed the rest. All via the staged manual procedure (not `run_sweep`: base can't equal the sweep branch; scripts/registry live on `sweep/naming-conventions`). Every deployed form passed an orphan gate (0 references to any renamed-away name, tags excluded) and a corpus re-scan confirms **0 unexpected high-conf violations** among deployed forms. Colliding rename targets (new name already in use / two sources → one target) are disambiguated with a short random uuid suffix (`PREFIX_<Camel>_<8hex>`). **NOT swept:** (a) **8 accepted-as-is exceptions** (owner decision 2026-07-21) — a single `name` is shared by two structurally-different nodes nested in the same branch (e.g. a plain section panel `PN_X` and, several levels below it, a repeatable row panel also `PN_X` that by convention "should" be `RCP_X`). The forms **work correctly**: every reference is either fully path-qualified (`…PN_Authorization.PN_X` vs `…_Table_DYN.PN_X`) or a repeat-only API (`.instanceManager`/`.instanceIndex`) that only the repeatable node has, so AEM resolves each to the right element. A global string rename would break the plain sibling, and a node-scoped rename is per-form fragile for a purely cosmetic prefix — so these are **left as-is, not renamed**: AABT_033, AACQ_019, AAHM_af_ab_019, AAHU_019, AAKL_019, AARW_033, BANR_033, BAXT_019. (b) **low-confidence repeat sub-panels — DONE** (2026-07-21): the `--include-low` pass renamed wrong-prefixed repeat-container sub-panels to the `RCP_` family across 117 forms (all deployed; 2 transient deploy-fails retried OK). A final corpus scan now reports **only the 8 accepted forms as affected** (71 residual violations, all within those 8) — every other previously-affected form is 0-violation. Because of (a), the detector will always report these 8 as affected — so before flipping to `swept`, add them to a documented accept-list (or scope the regression guard to exclude them). The 8 accepted forms were **partial-fixed** (2026-07-21): the fixer applied every fixable rename (skipping only the shared-name conflict) and deployed, leaving **exactly one accepted node each** — AABT_033 `PN_Relazione`, AACQ_019 `PN_Thirdparty_Account`, AAHM_af_ab_019 `ST_Subtitle0`, AAHU_019 `PN_GenericSignature`, AAKL_019 `PN_SignatureIndividual`, AARW_033 `TXT_ShareOfOwnership`, BANR_033 `TTL_Authorized_User`, BAXT_019 `TXT_Accountholder`. **Final corpus state: every form 0 actionable violations.** These 8 nodes are recorded in `find_naming_violations.py`'s `ACCEPTED` accept-list (keyed by form+name), so the detector reports them under `accepted` (not `affected`) and the regression guard stays green. To retire an exception later, fix the form (give the shared-name nodes distinct names) and drop its entry from `ACCEPTED`. **Fixer corrected + full redo (2026-07-22):** the reference-rewrite in `set_naming_prefix.py` was doing a whole-XML text replace (guarded only against XML tag positions), so it clobbered any *visible* string that happened to contain a renamed name — it turned `jcr:title="Investment"` into the field name, etc. Corpus impact was a single form, **AACQ_019** (one `jcr:title`: `PN_StpPortfolioNo` wrongly dragged to `RCP_StpPortfolioNo`); the bug bites new intake forms harder (real-word names in titles/labels). The fixer is now **attribute-scoped**: it rewrites only `name="…"` and `fd:*` rule/script references, never `jcr:title`/`jcr:description`/rich-text `_value`/dropdown `options`/`fd:translationIds`/`bindRef`. All **260 forms regenerated from the pre-sweep zips** with the corrected fixer (`--include-low`) and redeployed; the regenerated corpus is byte-identical (XML) to the previous deployment except AACQ_019's restored title (collision-uuid suffixes re-rolled, being nondeterministic).

## PROBLEM-default-translations — Add the engine's default UI translations missing from form dictionaries
**Symptom:** the conversion engine merges a fixed set of **default UI translations** into every form's translation dictionary at package-write time (`blueprint/core/src/aem/package_writer.rs::assemble_package`, from `profiles/ubs/aem/translations/{de,en,fr,it,sp}.toml`): the toolbar buttons (Back / Next / Submit / Preview / Save Progress / Add), the submission messages, the two Summary labels, and three custom-element labels (For internal bank use only / Signature(s) verified… / Company) — 15 master keys. Older / hand-built forms predate that step and are **missing some or all** of these entries from their per-locale dictionary files (`…/guideContainer/assets/dictionary/<locale>.xml`). This sweep adds the missing default entries to each form's **existing** dictionaries. **Engine-authoritative for this problem** (owner-directed: "these translations get added to the translation key xml files … add them if not present"); the TOMLs are vendored to `.claude/scripts/data/translations/` so the sweep is self-contained (refresh by re-copying if the engine's defaults change).

Two facts where the *deployed corpus* (the ground truth we edit) overrides the *current* engine source: (1) the entry element name is a **UUIDv3** (MD5, no namespace) of the raw master text — every existing `fd_<uuid>` in the corpus is v3, while today's `package_writer.rs` computes v5 (older-lineage / stale-binary split); (2) inside attribute values `>` is left **unescaped** (`&lt;p>…`) while `<`, `&`, `"` are escaped — matching the engine's `reformat_attributes`.

**Scope (strict engine mirror):** only the locales the engine emits defaults for — **de, de-ch (→de), en, fr, it, sp, es (→sp)** (config.toml `[language_synonyms]` de→de-ch, sp→es; master `en`). Regional extras that exist in some forms (en-us 25, en-gb 3, it-ch 4, fr-ch 1, default 4, pt 1) are **left untouched**. **Additive only:** an entry is inserted only if its `sling:key` (`fd_<master>`) is absent — an existing key is never overwritten (form-content translations take precedence, e.g. a form-authored `fd_Next`). Dictionaries are **never created** for a language the form lacks. **228 of 286 packages affected (2,670 missing entries); 58 packages have no translation dictionary at all → out of scope** (synthesising a whole dictionary is a larger change than filling existing ones; 0 already-complete — nearly every legacy form misses at least the custom-element labels).
**Detect:** script: `find_default_translations.py`  _(per form, per targeted existing dict file: which default `fd_<master>` keys are absent; affected = ≥1 missing; emits `missing:{locale:[master,…]}` as the review surface. Idempotent: a filled form reports 0. Forms with no targeted dictionary are counted `no_dictionary`, not affected.)_
**Fix:** script: `set_default_translations.py`  _(for each targeted existing dict file, inserts the missing entries before `</jcr:root>`, byte-formatted exactly like the engine (v3 element name, engine attribute escaping, message = the vendored translation for that locale's language). Rewrites only the changed dict entries in place via the `zip` CLI, like `form_xml.py` repack. Prints `{added, changed:{locale:count}, remaining, ok}`; `ok` = a re-scan self-check shows 0 default keys still missing.)_
**Verify:** the fix's own `ok` (0 default keys remain missing after the insert). **Note:** `check_form.py` / `form_diff.py` only read the form `.content.xml`, which this fix never touches — so a `must`/`must-not` regex or a line-count bound can't see the dictionary edits; the fix's `ok` is the real gate. `delta` (engine MCP) still applies — additive well-formed dictionary entries introduce no new schema violations.
**Max changed lines:** omit — the edits live in the dictionary files, invisible to `form_diff` (`.content.xml` only); gate on the fix's `ok`.
**Origin:** engine-diff (engine adds these defaults to every form; legacy forms lack them)
**Tracking:** #72
**Status:** swept 2026-07-23 (228/228 fixed + deployed, 0 flagged, PR #73 merged) — piloted (6) AAAA/AAAB/AAAE/AAAI/AAAL/AAAM_019 reviewed OK, then --continue for the remaining 222 fixed + deployed. Every form `fix ok:true` (0 default keys still missing on re-scan) and all targeted dictionary files well-formed; deploy verified via packmgr install. Run via the **staged manual/worktree procedure, not `run_sweep`** (scripts + registry live on `sweep/default-translations`, not master — master push is blocked; `run_sweep` would `git checkout master` and lose the branch-only scripts, and its end-of-run worktree can't share the branch the main checkout is on). Element-name UUIDs match the deployed corpus (v3/MD5, not the engine source's v5); additive-only, so no form-authored translation was overwritten. **Not flipped to `swept`:** the detector re-scans the corpus each run and will report these 228 as affected again if any regenerate from pre-sweep zips; before enrolling in the CI regression guard, confirm the deployed corpus is the source of truth (the 58 no-dictionary forms stay out of scope — they legitimately have no dictionary to fill). 0 already-complete going in (nearly every legacy form lacked at least the custom-element labels).

## PROBLEM-formconfig-private-person-default — Form configurator: preselect "Private Person" (RB_FormularAdressat)
**Symptom:** the form-configurator radio (`name="RB_FormularAdressat"`, the UBS custom radiobutton `ajila-forms-customers/ajila-forms-ubs/components/controls/radiobutton` choosing Private Person / Minderjährige / Firma / GbR) opens with **nothing selected** on most forms. QA wants **"Private Person" preselected on open** (option key `1`). Reported on AAAZ/AAGA/AAHM/BAGE/BAGH; generalizes across the corpus. **Origin: manual (QA/owner directive — the engine emits no initial value).**

**Mechanism = the `_value` property, NOT a `default` attribute.** The component's `widget.jsp` renders an option `checked` when `option.key == guideField.value`, and in this legacy guide runtime the field's initial value is `_value`. **Ground truth: 9 corpus forms already preselect Private Person via `_value="1"`** (AABO/AACP/AACR/AAFC/AAHD/AAHE/AANJ/AANM/AANN_019), and AANV_019 carries `_value="4"` for its GbR-only variant. **A static `default` attribute is NOT read by this runtime — verified empirically: `default="1"` deploys to the JCR (AEM-confirmed) but the radio still renders unselected.** (First pilot used `default` and failed the visual check; corrected to `_value`.)

**Uniform, single-lineage:** all 39 forms carrying the radio are entity `_019`, all the UBS custom radiobutton. **29 affected** (no `_value`); **9 already correct** (`_value="1"`); **1 skipped by design** — AANV_019 options are `[4=GbR]` only (no key `1`, already `_value="4"`), never forced to a non-existent option. A radio whose options lack key `1` is left untouched. Option-label text varies by count (`[1=Private Person,2=Minderjährige,3=Firma,4=GbR]` ×33, plus `[1=Private Person,3=Firma]`, `[1=Private Person,2=Minderjährige]`, `[1=Private Person,2=Firma]`) but key `1` is always Private Person. Feedback coverage: AAAZ_019/BAGE_019/BAGH_019 fixable; **AAGA** is PDF-only (not in corpus — the re-scanning detector catches it on intake); **AAHM**'s deployed form has no form-configurator radio (feedback stale / structural mismatch).
**Detect:** script: `find_formconfig_default.py`  _(affected = a form with an `RB_FormularAdressat` radio whose `options` include key `1` and whose `_value` != `1`; radios without key `1` reported under `skipped_no_option1`, never affected; emits per-form `{options, value}`. Idempotent: a form already `_value="1"` reports 0.)_
**Fix:** script: `set_formconfig_default.py`  _(quote-aware open-tag rewrite, like `set_attr_value.py`: on `name="RB_FormularAdressat"` tags whose options include key `1`, drop any existing `_value` and any stray `default`, then insert `_value="1"`; radios without key `1` untouched; prints `{matched, changed, skipped_no_option1, remaining, ok}` — `ok` = 0 option-1 radios still lack `_value="1"`; idempotent.)_
**Verify:** the fix's own `ok` (every option-1 `RB_FormularAdressat` now `_value="1"`) + a re-scan showing 0 affected + a pilot **visual check** (form opens with Private Person selected). `must`/`must-not` can't isolate one radio's `_value` corpus-wide, so `ok` + the visual check are the gate.
**Max changed lines:** 2   _(one attribute inserted on one tag = +1 add; form_diff may count the reflowed tag line as remove+add = 2. Flag anything more.)_
**Origin:** manual (QA/owner directive; engine emits no initial value)
**Tracking:** #74
**Status:** swept 2026-07-23 (29/29 fixed + deployed, 0 flagged, PR #75 merged) — piloted (3) then --continue — mechanism corrected to `_value` (first pilot's `default="1"` deployed but did not preselect; owner confirmed the `_value="1"` re-pilot renders Private Person selected). Detector: 29 affected / 9 already-correct (`_value="1"`) / AANV_019 skipped (`[4=GbR]`, `_value="4"`). Every form `fix ok:true` + deploy verified via packmgr install; the fixer also stripped the stray `default` from the first attempt. Run via the **staged manual/worktree procedure, not `run_sweep`** (scripts + registry live on `sweep/formconfig-private-person-default`, not master — master push blocked). Not yet flipped to `swept` (flip at merge). AAGA (PDF-only) will be caught by the re-scanning detector on intake; AAHM's deployed form has no form-configurator radio.

## PROBLEM-save-as-draft-menu — Enable "Save as draft" (menuOptions on the DAM asset)
**Symptom:** QA wants **"Save as draft"** enabled — the complement to removing the Save Progress button ([PROBLEM-nav-save-progress-button](#) / #50): *"hide the button and go to properties → UBS → select Save as draft"* (AABT/AADB/AAEH/AAPC). **Origin: manual (QA/owner directive).**

**Mechanism = `menuOptions` on the DAM asset metadata, NOT the guideContainer.** "Properties → UBS → Save as draft" is the multi-value `menuOptions` property on the form's **DAM asset metadata** node (`…/dam/formsanddocuments/…/AF_<code>/.content.xml`, the `<metadata>` under `dam:AssetContent`). Confirmed from the UBS app: the metadata-schema dialog (`apps/.../adaptiveform/extendedmetadata/.content.xml`) defines the item `text="Save as draft"` → `value="ajila-forms-ubs-menu-option-save"`, and `clientlibs/.../ubs-menu.js` maps that option to `saveAsDraft()`. So "enabled" ⇔ `menuOptions` contains `ajila-forms-ubs-menu-option-save`. (This edits the **DAM entry**, a different ZIP entry than the form `.content.xml` — like [PROBLEM-default-translations](#); the guideContainer autosave config — `autoSaveStrategyType`, `autoSaveInfo` — is uniform across all 286 forms and is NOT the toggle.)

**Extent:** **123 of 286 packages lack it** (no `menuOptions`); **163 already carry it** (`menuOptions="[ajila-forms-ubs-menu-option-save]"` — ground truth). No form uses the other schema option (`…-goto-start`), so `menuOptions` is absent or exactly `[…-save]`. Feedback forms AABT_033/AADB_019/AAEH_019 lack it; **AAPC_033 already has it** (already correct). **Scope note:** whether to enable on all 123 vs a subset is an owner decision (some forms may intentionally omit drafts) — pilot the feedback forms, confirm scope before `--continue`.
**Detect:** script: `find_save_as_draft.py`  _(affected = the DAM asset metadata's `menuOptions` does not contain `ajila-forms-ubs-menu-option-save`; emits per-form current `menuOptions`. Idempotent: a form already carrying it reports 0. `no_asset` = no DAM asset metadata found, not affected.)_
**Fix:** script: `set_save_as_draft.py`  _(on the DAM asset `<metadata>` open tag: insert `menuOptions="[ajila-forms-ubs-menu-option-save]"` if absent, or append the save option inside an existing `[…]`; rewrites the DAM entry in place via the `zip` CLI. Prints `{inner, changed, before, after, ok}`; `ok` = the metadata now lists the save option; idempotent.)_
**Verify:** the fix's own `ok` (metadata now lists the save option) + a pilot **visual check** (the rendered form's menu now offers "Save as draft"). `check_form.py`/`form_diff.py` read only the form `.content.xml`, which this fix never touches, so they can't see the DAM edit — `ok` + the visual check are the gate (as with [PROBLEM-default-translations](#)).
**Max changed lines:** omit — the edit is in the DAM asset entry, invisible to `form_diff` (`.content.xml` only); gate on the fix's `ok`.
**Origin:** manual (QA/owner directive; complement to #50)
**Tracking:** #76
**Status:** swept 2026-07-23 (123/123 fixed + deployed, 0 flagged, PR #77 merged) — piloted (3) then --continue — pilot AABT_033/AADB_019/AAEH_019, then the remaining 120. Every form `fix ok:true` + deploy verified. Cross-checked against the 163 ground-truth forms: `menuOptions` is the SOLE systematic difference and all 163 use exactly `[ajila-forms-ubs-menu-option-save]`, so the fixed forms are now byte-identical in this respect. **"Save as draft" is a runtime eBanking-shell menu item (ubs-menu.js) — it does NOT render in the AEM author preview on ANY form (owner confirmed a known-good form also hides it in preview), so verification is authoring parity, not a preview visual.** Enabled on ALL 123 lacking forms (owner: "if everything matches, continue"). Run via the **staged manual/worktree procedure, not `run_sweep`** (scripts + registry on `sweep/save-as-draft-menu`, master push blocked). Not yet flipped to `swept` (flip at merge).

## PROBLEM-formmetadata-step — Every form needs the FormMetadata fragment as its first step
**Symptom:** the form must carry the global **FormMetadata fragment as a wizard step** — a fragment node (not a panel wrapping one) directly under `guideContainer › rootPanel(guideRootPanel) › items`, hidden and DoR-excluded. Canonical shape (reference: AAOV_033): `fragRef="/content/dam/formsanddocuments/afforms_global_fragmentlib/formmetadata"`, `sling:resourceType="fd/af/components/panel"`, `guideNodeClass="guidePanel"`, `name="FormMetadata"`, `jcr:title="FormMetadata"`, `visible="false"`, `dorExclusion`/`dorExcludeTitle`/`dorExcludeDescription="true"`, `textIsRich="true"`, children `layout` + empty `items`, **first step**. Forms either lack it or have it with the wrong shape, so the rule inserts *or* normalises.
**Detect:** script: `find_formmetadata_step.py`  _(`insert` = no FormMetadata step; `normalise` = present but a canonical attribute is wrong or it is not first. Flags, never auto-fixed: `misplaced`, `duplicate`, `no_items`, `extra_children`. `--form <F>`, `--verbose`. Idempotent.)_
**Fix:** script: `set_formmetadata_step.py`  _(inserts the canonical node as the root `items`' first child, or normalises an existing one in place and moves it to the front. Attributes and children outside the canonical set are kept, not stripped. `--dry-run`. Prints `{inserted, normalised, moved, attrs, kept_extra_attrs, extra_children, skipped, well_formed, canonical_first, ok}`; `ok` = parses AND canonical AND first.)_
**Verify:** the fix's `ok` + a re-scan showing 0 affected + **#18 and #22 staying green** + a **visual check in AEM** (the form still opens on its real first page; no extra step in the wizard).
**Max changed lines:** omit — an insert adds the node, a normalisation edits attributes, a move relocates it; gate on `ok` + the re-scan.
**Manual follow-up:** a `panel_title…` child inside the fragment node is kept and flagged (`extra_children`); removing it is a human decision.
**Origin:** manual (owner directive; reference = AAOV_033)
**Tracking:** #88
**Status:** open

## PROBLEM-step-title-single — Exactly one `css="stepTitle"` h2 title per wizard step
**Symptom:** each wizard step must render **one** step title. [PROBLEM-step-title-panel](#) / #18 wraps the title and [PROBLEM-jump-to-field-button](#) / #22 puts the jump button on that panel, but neither asserts **how many** nodes carry `css="stepTitle"` (#18 deliberately keys on `headingLevel="2"`), so a step can carry the marker on its wrapper panel, on sibling section panels **and** on a later h2 draw at once → problem with margins.

**Rule (owner, 2026-07-27): the canonical title is the FIRST `headingLevel="2"` draw in document order** in the step, at any depth, marked or not — it is the heading the reader sees first. The fixer puts the marker there (**adding** it if missing, so a marker on a wrong node is *moved*, not just deleted) and strips the `stepTitle` **token** from every other marked node, preserving any other class. No node, title text or other attribute is touched. First-in-order is also what #22 assumes (`title_panel()` takes `h2_draws(step)[0]`), so this **removes** a disagreement between the two sweeps; #18 never reads `css`. Both neighbour detectors report 0 affected after this rule runs.

**Extent (286 packages / 1,069 steps):** `dupe` **329** → **150 forms** (83 of them need the marker moved onto a previously-unmarked first h2) · `ok` 615 · `flag:unmarked` 91 (a title exists but nothing in the step is marked — #18/#22 territory) · `flag:ambiguous` 5 (marked nodes, no h2 at all) · `titleless` 29.

**⚠️ Open decision — do not sweep until it is made.** Of the 527 nodes that lose the marker, 166 are the step panel itself, 150 other panels and 115 later h2 draws (all uncontroversial: a panel renders no heading, a later h2 is not the first). The remaining **96 are `ST_*` textdraws that are NOT headings** (94 with no `headingLevel`, 2 with `headingLevel="3"` — `ST_JointAccount`, `ST_Statement_1..5`, `ST_Sub_Tile_Tax_resid`…). They are *styled* as step titles on purpose or by copy-paste; dropping the class changes how they look, which is a human call. Options: strip them too / restrict the fixer to panels + h2 draws / strip and restyle with a proper sub-heading class.
**Detect:** script: `find_step_title_dupes.py`  _(per-step verdicts above; emits `fix[] {form,step,keep,strip[]}` + `flag[] {…,reason}` as the review surface. `affected` = forms with >=1 `dupe`, so a flag never blocks a form. `--form <F>`, `--verbose`. Idempotent.)_
**Fix:** script: `set_step_title_single.py`  _(byte-offset splices, back-to-front; strips the token — dropping an emptied `css` — and adds it to the first h2 when absent. The insert handles both single-line and multi-line open tags. Prints `{steps_fixed, stripped, marker_moved, flagged, remaining, well_formed, ok}`; `ok` = parses AND 0 `dupe` steps remain.)_  Shared classifier in `step_title_dupes.py` (expat + byte offsets + quote-aware tag scan, so a literal `>` in a rich `_value` can't derail an edit).
**Verify:** the fix's `ok` + a re-scan showing 0 `dupe` + an **AEM visual check on a form from the 83 "marker moved" set** — that group changes *which* heading is styled as the step title, and no script can assert it. The first pilot passed every automated gate while marking the wrong node (see the PR body); that is why this line matters.
**Max changed lines:** omit — a variable number of per-step token edits; gate on `ok` + the re-scan.
**Origin:** manual (owner/QA directive 2026-07-27 — an invariant #18/#22 assume but never enforced)
**Tracking:** #83
**Status:** open
**Prerequisites for a sweep:** review of the three scripts, a Tracking issue, the `ST_*` decision above, a ~6-form pilot across both panel lineages. Overlaps #18/#22 `_merged.zip`s → sequence the rollout. Tested end-to-end through the intake app on BAGH_019 (2 `dupe` + 2 `ok`, 0 flags; step 1 moves the marker to `TTL_FormConfiguar` and strips it from `PN_FormConfigurator`, `PN_AccountHolder` and `TTL_Kundendaten`).

## PROBLEM-default-label-leak — Wrong-language leak of engine-default labels (FLAG-ONLY report)
**Symptom:** some forms authored a UI label (`jcr:title`/`custom`) as the **non-English translation** of one of the 15 engine-default strings, so its dictionary key is `fd_<foreign text>` — which the form's *other* languages don't translate, leaking the wrong language on those versions. E.g. nav buttons `jcr:title="Avanti"`/`"Indietro"`/`"Anteprima"` (Italian) show Italian on the DE/EN/FR versions; a company field `jcr:title="Firma"` (German "Company") shows German on the EN/IT versions; a panel `jcr:title="Solo per uso interno della banca"` (Italian "For internal bank use only"). **Origin: manual (owner directive).**

**FLAG-ONLY — no automated fix (owner directive 2026-07-23).** This entry only *detects and reports* the leaks; it does **not** re-key or edit forms. An earlier auto-fix (re-key the authored text to the English master so [#72](#)'s dictionary entries render it) was **removed** because it is unsafe: `fd_<master>` is not always correctly translated — some dictionaries carry an untranslated **stub** (`message == English`, which #72's additive fill never overwrites, e.g. `fd_Company`="Company" in a de dict → German would show "Company"), a **form-specific variant** (`fd_Company`="Razón social" in an es dict), or is **missing** (#72's strict-mirror `it-ch` → would fall back to the English master). A pilot hit exactly this on AAGK_019 (German rendered "Company"), so the fixer was scrapped. The correct target text is a **translation decision**, so leaks are handed to the translation team, not auto-changed.

**Extent (report):** **52 of 286 packages** carry ≥1 leaking default-string label. Leaks: Avanti/Indietro/Anteprima (Italian nav) 29 each, Firma→Company 22, Weiter/Zurück/Vorschau (German nav) 2 each, Solo per uso interno della banca 4, Riepilogo delle informazioni del modulo 1. 1 label sits on a path-`translationId` element (listed under `flagged`).
**Detect:** script: `find_default_label_leak.py`  _(flag-only report: per form, the `jcr:title`/`custom` labels that are a non-English default translation rendering wrong in ≥1 locale, with `wrong_in` locales + the target English `master`; plus a `flagged` list for path-translationId labels. Reads `default_label_leak.py` (reverse map of the 15 defaults' translations, unambiguous only).)_
**Fix:** manual — hand the report to the translation team: for each leaking label, add the correct per-locale translation to the form's dictionaries (or re-author the label in the master language once the dictionaries are sound). **No fix script** (removed 2026-07-23).
**Verify:** n/a (no fix). The report itself is the deliverable.
**Origin:** manual (owner directive; wrong-language leakage of engine-default strings)
**Tracking:** #78
**Status:** open — **flag-only report tool**, not a run_sweep sweep and NOT enrolled in the CI regression guard (it would otherwise fail forever on the 52 known leaks). The removed auto-fixer's regression (AAGK_019 German "Company", from a pre-existing `fd_Company` stub #72 left) is why this is detect-only. Related follow-up: a #72 **stub-repair** sweep (overwrite `message==English` dictionary stubs with the correct TOML translation) would fix the "English/foreign-shows-in-<lang>" family at the dictionary level and is the sound prerequisite before any label re-key could be considered.

## PROBLEM-signature-panel-ref — Signature name-calc must resolve its repeatable panel
**Symptom:** on the signature page the signer's name should pre-fill from the first/last name entered earlier; on many forms it silently stays empty. The signature fragment resolves the person's repeatable data panel **by name** (`var pnIndividual = PN_AHRP.instanceManager.instances[…].PN_IndividualBasic.PN_Name_Individual;` + `//Expecting Account Holder panel fragment with name PN_AHRP`). These forms carry **no `bindRef`** — the component `name` *is* the binding — so when the host form calls that panel `RCP_AHRP` the identifier is undefined, the calc throws `ReferenceError`, and the field stays blank. **Owner directive 2026-07-27: the fragment's name wins** — rename the host form's panel, accepting that it breaks the repeat-panel `RCP_` convention. That requires the `SIGNATURE_CONTRACT_NAMES` exemption in `find_naming_violations.py`; **without it the guard flags the renamed panel and `apply_sweeps.py` reverts the fix** (verified both ways). **Ordering: the exemption must land with this entry.**

**Two modes, because the fragment is sometimes inlined and sometimes referenced.** (1) *Script* — where the conversion inlined the fragment, the name-calc is visible and states its own contract. (2) *Contract* — where the form only carries a `fragRef`, the calc lives inside the fragment and is absent from the package, so the name comes from **[signature-fragment-contract.md](signature-fragment-contract.md)**: 15 rows **derived from the fragment library sources** by `extract_signature_contract.py`, never hand-maintained, keyed `<library>/<fragment>` because the same fragment name differs per library (`affrg_UBSEuropeSignature1` resolves `PN_EURP` in germany, `PN_AHRP` in italy). AAAZ_019 is this case. **The table never overrides direct evidence:** some forms inlined a fragment version that expected `RCP_` (AAHG_019 reads `RCP_AHRP` and resolves), so a role already satisfied by the form's own script is skipped.

**The fragment's code is authoritative, not its comment.** 5 of the 15 fragments name a different panel in their `//Expecting …` comment than the one their code resolves (germany/`affrg_ARSignature1` resolves `PN_ARP`, comment says `PN_LGA`), so the comment is used only as a scope signal. `affrg_SignatureGeneric1` resolves two panels and is excluded from the table — no single answer, so no guess.

**Only the root identifier is checked.** `.instanceManager` exists only on a repeatable panel, so that name must be in this form. The rest of the path (`…PN_Name_Individual.TXT_LastName`) lives inside the fragment, is not shipped in the package, and is never asserted — which is why the pilot needs a human visual check. Scope is limited to the name-retrieval calc: checking every `X.instanceManager` yields **635 false positives across 186 forms** (scripts legitimately drive panels inside fragments). Inferring the target from the only person-details fragment panel was also rejected — it proposed `RCP_ARP`→`PN_AHRP` on AABV_019, i.e. the wrong person's name.

**Extent: 58 of 286 packages actionable — 82 renames** (`RCP_AHRP`→`PN_AHRP` ×57, `RCP_LGA`→`PN_LGA` ×24, `RCP_ARP`→`PN_ARP` ×1). Verified by simulating the whole sweep: all 58 report `ok`, 82 renames applied, and a re-scan then reports **0 affected** — so the guard returns to green on its own, with no per-form accept-list to maintain.

**A further 109 findings across 84 forms are NOT auto-fixable and are reported under `flagged`, never as affected.** 12 are provably impossible (the form has no repeatable panel at all, so nothing `.instanceManager` could bind to); the rest **need human revision and a fix may well exist** — repeatable panels are present but none matches the wanted role token, and the detector's person-details heuristic looks for a `fragRef`, so it misses panels with inlined content (e.g. AAAB_019 carries `RCP_RCP_StpLastNameFirstName`, plausibly the right target). They are a review queue (`--flagged`), not failures, and not suppressed data: the same split as [PROBLEM-default-label-leak](#), which is report-only for exactly this reason.
**Detect:** script: `find_signature_panel_ref.py`  _(per unresolved identifier a verdict: `rename` = exactly one repeatable panel matches by **role token** (name minus prefix: `RCP_AHRP` → `AHRP` ← `PN_AHRP`) and its name is unique; else `flag:no-target` / `ambiguous-target` / `shared-name` / `unprefixed-identifier`. **Affected = ≥1 `rename`**, i.e. exactly what the Fix repairs — so the sweep has no no-ops and the guard clears itself; non-actionable findings go to `flagged`. Idempotent: after the fix the identifier resolves and the finding disappears.)_
**Fix:** script: `set_signature_panel_ref.py`  _(renames the panel and rewrites every reference, attribute-scoped like `set_naming_prefix.py`: `name=` plus `fd:*` code only, never `jcr:title`/`_value`/`options`/`fd:translationIds`/`bindRef`. Applies only `rename` verdicts; the rest are listed under `skipped`. Prints `{inner, renamed, skipped, remaining, ok}`; a flag-only form reports `ok:true, n_renamed:0` — not a failure.)_
**Verify:** the fix's `ok` + a re-scan showing 0 actionable + `find_naming_violations.py` staying green (the exemption's job) + a pilot **visual check in AEM: the signature page pre-fills the name.** That check is the real gate — a resolving identifier proves the reference is valid, not that the fragment's inner path returns data.
**Max changed lines:** omit — one rename touches the definition plus every reference, so the count scales with reference density (AAAZ_019: 8 lines for 2 renames). Gate on the fix's `ok` + the re-scan.
**Origin:** manual (owner directive — the fragment's hardcoded name wins over the `RCP_` convention)
**Tracking:** #80
**Status:** open — **not yet run; 58 forms are waiting.**
_Enrolment: the registry parsers (`check_regressions.py`, `apply_sweeps.py`, `intake/core.py:swept_rules`) test whether the **first line** of this field contains "swept", so that word is kept off line 1 deliberately — enrolling now would put the CI guard red on the 58 pending forms. Validated on AAAZ_019 (`RCP_AHRP`→`PN_AHRP`, `RCP_LGA`→`PN_LGA`; `ok:true`, 8 changed lines, no leftover references, display attributes byte-identical, re-detect clean, guard green) and on a synthetic Mode-1 form. **Rollout:** `run_sweep.py --slug PROBLEM-signature-panel-ref --only AAAZ_019` → deploy → visual check → `--continue` for the remaining 57 → change line 1 to `swept <date> (58/58 …)`, which also switches the rule on in the intake app. Re-run `extract_signature_contract.py` whenever the fragment library changes; the affected set moves with the table._

## PROBLEM-banking-relationship-margin — Banking-relationship wrapper panel must carry `css="ubs-margin-20"`
**Symptom:** the panel **containing** the banking-relationship fragment — the `PN_BR` wrapper from [PROBLEM-banking-relationship-wrap](#) / #29 — must carry the layout class **`css="ubs-margin-20"`** so the banking block gets the standard vertical spacing. `set_banking_wrap.py` synthesises the wrapper with no `css` at all, so every wrapper it created is affected. Target is the **wrapper**, not the `fragRef` panel #10 owns (owner-confirmed 2026-07-27).
**Extent:** **259 of 286 packages** carry a `PN_BR`; **none** has the class (`already_ok: 0`). The other 27 have no wrapper (later-page banking / no banking) and are out of scope — a form with no `PN_BR` reports clean here.
**Depends on #29 — run it first.** A form whose first-page banking panel is unwrapped reports clean here, but #29 reports it **red**, so it can never look all-green; and `apply_sweeps.py`'s fixpoint loop wraps (#29 is earlier in this file) then adds the class in the same pass. Deliberately left implicit rather than adding a script that detects on the fragment.
**Detect:** script: `find_missing_attr_on.py --select name="PN_BR" --require-attr css=ubs-margin-20`  _(affected = a `PN_BR` open tag lacking the class; the selector includes the closing quote so `PN_BRxxx` can't match. **No quotes** — see the parser traps at the top of this file.)_
**Fix:** script: `set_attr_on.py --select 'name="PN_BR"' --set-attr css=ubs-margin-20`  _(**quotes required** here: the Fix verb is `shlex.split()`. Note `set_attr_on.py` replaces a `css` value wholesale — it does not merge class tokens. Harmless today since no wrapper has any `css`, but re-check the detector's list before a rerun.)_
**Verify:** the fix's `all_ok` + a re-scan showing 0 affected + a pilot **visual check** (banking block has the standard margin, #29's summary/DoR exclusions still hold). A corpus-wide `must:` on the class is too weak — it occurs on other panels.
**Max changed lines:** 2   _(attribute insert = +1; 2 if an existing `css` value is replaced)_
**Origin:** manual (owner directive 2026-07-27, as an addition to #10)
**Tracking:** #82
**Status:** open
**Piloted** on BAGH_019 locally: `matched:1, added:1, ok:true`, 2 diff lines, package otherwise byte-identical, re-scan clean, second pass a no-op. That pilot also turned up the `set_attr_on.py` single-line-tag bug and the Detect-quoting trap, both fixed alongside this entry — the PR body has the full write-up.

## PROBLEM-toolbar-nav-handler — Submit and Back must run the UBS navigation helpers
**Symptom:** the wizard toolbar's **Submit** button submits through the rule editor's built-in **Submit Form** action, bypassing the UBS validation/error path, and the **Back** button has no handler at all. UBS directive (2026-07-28, definitive, all forms): both must call the UBS navigation helpers, as the reference form **AAOV** (italy) already does. Target shape per button — an empty `<fd:rules>` plus an `<fd:scripts>` child whose `fd:click` holds one SCRIPTMODEL:
`toolbar.submit` → `window.forms.ubs.navigation.submit(submitErrorMessage);` · `toolbar.previtemnav` → `window.forms.ubs.navigation.previousStep(this);`

**`nextitemnav` is deliberately OUT OF SCOPE.** 285 of 286 forms run `window.com.ajila.forms.ubs.navigation.nextStep(this)` — the superseded namespace — and UBS did not ask for it. Including it "for consistency" would rewrite the whole corpus for no requested benefit, so the corpus stays intentionally mixed: Next on the old namespace, Submit/Back on the new. Raise it with UBS separately if that matters.

**Four states occur, each a different edit.** `replace-rule` **260** — a visual rule (`SUBMIT_FORM`) in `fd:rules`, no script: the rule is dropped and the script added. `add-node` **282** — the Back button element is *self-closing*, so it is opened and given an `<fd:scripts>` child. `replace-script` **27** — a non-canonical payload (25 submit + 2 back on the old namespace). `ok` **1**. `skip:no-button` **2** — no Back button at all; never fabricated, a single-step form legitimately has none. `fd:navigationChange`, present on all 286 submits, is never touched.

**Conformance is an exact payload match**, so all forms converge on one byte-identical value — which also normalises a stale editor `field` path (2 forms carry `guide.Preview.toolbar.previtemnav`). Wholesale replacement is safe because every existing payload holds exactly **one** script, verified corpus-wide; a payload with more than one is `flag:multiple-scripts` and left alone, as is any click script we did not author (`flag:unexpected-script`). Both are 0 today.

**Extent: 286 of 286 packages, 569 edits.** Simulated end to end: 286/286 `ok`, re-scan reports **0 affected** (570 `ok`, 2 `skip`), `fd:navigationChange` intact on all 286, 0 `SUBMIT_FORM` rules left, every display attribute (`jcr:title`/`_value`/`options`/`fd:translationIds`/`bindRef`/`css`) byte-identical, and `find_naming_violations.py` still 0 affected. The generated payload is **byte-identical to AAOV's**, and AAOV itself classifies as `ok` on both buttons — the reference validates the rule.
**Detect:** script: `find_toolbar_nav_handler.py`  _(per button a verdict: `ok` = `fd:click` equals the canonical payload; else `replace-rule` / `replace-script` / `add-node` / `add-attr`, or `flag:unexpected-script` / `flag:multiple-scripts` / `skip:no-button`. Affected = ≥1 actionable verdict, i.e. exactly what the Fix repairs, so the guard clears itself. Idempotent.)_
**Fix:** script: `set_toolbar_nav_handler.py`  _(quote-aware open-tag scan — the JSON and rich-text values contain literal `>` — with byte-offset splices applied back-to-front. Drops the `fd:rules` visual rule, writes the canonical `fd:click`, and opens a self-closing button to add the child node, deriving indentation from the form itself. Prints `{inner, applied, skipped, remaining, ok}`; `ok` = well-formed AND nothing actionable left.)_
**Verify:** the fix's `ok` + a re-scan showing 0 affected + `fd:navigationChange` still present + a pilot **functional check in AEM: actually submit a form and confirm it arrives, and click Back through the wizard.** This replaces the native submit action with a JS helper on 260 forms — if the helper does not submit, those forms stop submitting. A visual check is not enough; the submission must be exercised.
**Max changed lines:** 8   _(both buttons = 2 removed + 6 added; 281 forms hit exactly 8, the rest 2–6. Flag anything more.)_
**Origin:** manual (UBS directive 2026-07-28; reference form AAOV)
**Tracking:** #87
**Status:** open — **not yet run; 286 forms are waiting.**

## PROBLEM-datepicker-current-date-default — Uncheck "Set current date as default value" on date pickers
**Symptom:** date pickers carry `defaultToCurrentDate="true"`, so the field opens pre-filled with today's date. UBS wants it unchecked: the user must enter the date deliberately, and a pre-filled today silently becomes wrong data on any form not submitted the same day. **Origin: manual (UBS directive).**

**The unchecked state is the attribute being ABSENT, not `false`.** Across the corpus `defaultToCurrentDate` occurs **only** with the value `true` — never `false` — so the 436 date pickers that do not pre-fill simply lack it, and removing the attribute reproduces exactly the shape the engine emits when the box is unchecked. Same mechanical shape as [PROBLEM-dor-exclude-hidden](#), which is why this entry needs **no new code**: it reuses the generic `find_affected.py --grep` detector and `remove_attr.py` fixer.

**Scoped by construction.** The attribute appears on **nothing but date pickers** (both `sling:resourceType` variants — `datepicker` 509 nodes and `guidedatepicker` 43), so `--attr defaultToCurrentDate` cannot touch another component type. No selector is needed.

**Extent: 71 of 286 packages, 116 attributes.** Both entities (64 nodes on `_019`, 52 on `_033`). Most forms carry one; a few carry several (AAUS_033 and BBES_033 have 8 each). Simulated end to end: 71/71 forms fixed, 116 attributes removed, 0 failures, every form still well-formed and passing the `must-not`, and a re-scan reports **0 affected**.
**Detect:** grep: `defaultToCurrentDate="true"`
**Fix:** script: `remove_attr.py --attr defaultToCurrentDate`
**Verify:** must-not: `defaultToCurrentDate=` _(the fixer prints no `ok` key, so `run_sweep` gates on this regex — same as PROBLEM-dor-exclude-hidden. Plus a pilot **visual check in AEM: open a step with a date picker and confirm the field is empty, not today's date.**)_
**Max changed lines:** 8   _(one line removed per attribute; 50 forms change 1 line, and AAUS_033/BBES_033 change 8. Flag anything more.)_
**Origin:** manual (UBS directive — a pre-filled current date becomes wrong data)
**Tracking:** _pending — create the issue and put the number here before the first `run_sweep`._
**Status:** open — **not yet run; 71 forms are waiting.**
_No new code: reuses `find_affected.py --grep` and `remove_attr.py --attr`, per the registry's "no code only if it reuses an existing Detect/Fix script" rule. Enrolment: the parsers key off the word in the **first line** of this field, kept off it deliberately — enrolling now would put the CI guard red on all 71._


